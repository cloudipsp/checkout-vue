import { createConfig } from '@/utils/helpers'
import names from '@/config/css-class-names'

export default createConfig(names, [
  'light', // btn_pay_wallet_variant
  'white', // btn_pay_wallet_color
])
